/*

import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import { getUserDetailByUUID } from "../../services/user-service";
import { AspectRatio, Button, Card, CardContent, IconButton, Typography } from "@mui/joy";
import {  StarRate } from "@mui/icons-material";






export function User() {
/*  const user = useParams();

  const [userInfo, setUserInfo] = useState<IEstudiantes | null>(null);

  useEffect(() => {
    loadData();
  }, []);

  const loadData = async () => {
    const data = await getUserDetailByUUID(user.uuid || "");

    setUserInfo(data[0]);
    console.log(data);
  };  */
/*
  return (
    <>
     <h1>Class of Music</h1>
     <br />
     <br />
     <table>
      <tr>
          <td>
             <Card sx={{with: 320}}>
             <Typography level="title-lg">Yosemite National Park</Typography>
             <Typography level="body-sm">April 24 to May 02, 2021</Typography>
             <IconButton
                  aria-label="bookmark Bahamas Islands"
                  variant="plain"
                  color="neutral"
                  size="sm"
                  sx={{ position: 'absolute', top: '0.875rem', right: '0.5rem' }}
        >
          <StarRate />
        </IconButton>
        <AspectRatio minHeight="120px" maxHeight="200px">
        <img
          src="https://images.unsplash.com/photo-1527549993586-dff825b37782?auto=format&fit=crop&w=286"
          srcSet="https://images.unsplash.com/photo-1527549993586-dff825b37782?auto=format&fit=crop&w=286&dpr=2 2x"
          loading="lazy"
          alt=""
        />
      </AspectRatio>
      <CardContent orientation="horizontal">
      <div>
          <Typography level="body-xs">Total price:</Typography>
          <Typography fontSize="lg" fontWeight="lg">
            $2,900
          </Typography>
        </div>
        <Button
          variant="solid"
          size="md"
          color="primary"
          aria-label="Explore Bahamas Islands"
          sx={{ ml: 'auto', alignSelf: 'center', fontWeight: 600 }}
        >
          Explore
        </Button>
      </CardContent>
             </Card>
          </td>
      </tr>
      <tr>
          <td>

          </td>
      </tr>
      <tr>
          <td>

          </td>
      </tr>
     </table>
    </>
  );
}  */
